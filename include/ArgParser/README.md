# Argument Parser (ArgParser)

Mix of 2 different kind of parser. **static/stack parser, and dynamic parser**
st_parser (a.k.a stack parser) completely uses data defined from user,
while dy_parser (a.k.a dynamic parser) in the other hand uses heap for flexibility.

